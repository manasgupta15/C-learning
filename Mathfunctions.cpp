#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    double x = 3.99;
    double y = 4;
    double z;

    // z = max(x,y);
    // z = min(x,y);
    // z = pow(2,3);
    // z = sqrt(9);
    // z = abs(-1);
    // z = round(x);
    // z = ceil(x);
    // z = floor(x);
    cout << z << endl;
    return 0;
}
